using Microsoft.EntityFrameworkCore;

namespace SeeLive.Domain.Tests.Features.Artists
{
    public class CreateArtistCommandHandlerTests
    {
        public CreateArtistCommandHandlerTests()
        {

            //var dbOptions = new DbContextOptionsBuilder<SeeLiveContext>()
            //    .UseInMemoryDatabase(Guid.NewGuid().ToString()).Options;
            //var seeLiveContext = new SeeLiveContext(dbOptions, );
        }
    }
}
